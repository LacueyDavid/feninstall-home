# Compatibility contract consumed by create_feniso install-config.
# Bump FENNOS_CONFIG_COMPAT_VERSION only when breaking bootstrap contract changes occur.
FENNOS_CONFIG_COMPAT_VERSION=1

# Minimum bootstrap version accepted by this config repository.
FENNOS_CONFIG_MIN_BOOTSTRAP_VERSION=1
